apiVersion: v1
kind: Service
metadata:
  name: currency-service
spec:
  ports:
  - name: currency-service
    port: 7000
    protocol: TCP
    targetPort: 7000
  selector:
    app: currency-deploy
  type: ClusterIP
status:
  loadBalancer: {}
