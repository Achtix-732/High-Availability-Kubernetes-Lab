apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: shop-demo-tls
  namespace: default
spec:
  secretName: shop-demo-tls
  issuerRef:
    name: selfsigned-issuer
    kind: ClusterIssuer
  dnsNames:
  - shop-demo.achtix.com # เปลี่ยนเป็นโดเมนจริงของคุณ
